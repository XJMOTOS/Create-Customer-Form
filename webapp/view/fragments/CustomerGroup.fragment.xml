<core:FragmentDefinition
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns:forms="sap.ui.layout.form"
	xmlns="sap.m">
	<forms:SimpleForm
		labelSpanXL="4" labelSpanL="3" labelSpanM="4" labelSpanS="12"
		emptySpanXL="0" emptySpanL="4" emptySpanM="0" emptySpanS="0"
		columnsXL="2" columnsL="1" columnsM="1"
		editable="false"
		layout="ResponsiveGridLayout"
		adjustLabelSpan="false"
		singleContainerFullSize="false">
		<forms:content>
			<Label
				class="sapUiTinyMarginTop"
				required="true"
				text="{i18n>lblCustomerGroup}"/>
			<HBox>
				<core:Icon
					decorative="false"
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					tooltip="{i18n>tooltipCustomerGroup}"
					src="sap-icon://hint">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					change="onToggleValidationButton"
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/0}"
					valueState="{= ${customer>/customerGroup/0} ? 'None' : 'Error' }"
					width="100%">
					<core:Item key="{customerGroups>Kdgrp}" text="{customerGroups>Kdgrp}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 1"/>
			<HBox>
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup1}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					selectedKey="{customer>/customerGroup/1}"
					items="{customerGroups1>/}"
					width="100%">
					<core:Item key="{customerGroups1>Kvgr1}" text="{customerGroups1>Bezei}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 2"/>
			<HBox>
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup2}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups2>/}"
					selectedKey="{customer>/customerGroup/2}"
					width="100%">
					<core:Item key="{customerGroups2>Kvgr2}" text="{customerGroups2>Bezei}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 3"/>
			<HBox>
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup3}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups3>/}"
					selectedKey="{customer>/customerGroup/3}"
					width="100%">
					<core:Item key="{customerGroups3>Kvgr3}" text="{customerGroups3>Bezei}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 4"/>
			<HBox>
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup4}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups4>/}"
					selectedKey="{customer>/customerGroup/4}"
					width="100%">
					<core:Item key="{customerGroups4>Kvgr4}" text="{customerGroups4>Bezei}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblDPUBStatus}"/>
			<HBox>
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup5}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{GWD_C_FORM_UI5>/ZFA1_DPUBStatusSet}"
					selectedKey="{customer>/customerGroup/5}"
					width="100%">
					<core:Item key="{GWD_C_FORM_UI5>KVGR5}" text="{GWD_C_FORM_UI5>BEZEI}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<!-- <Label
				text="{i18n>lblCustomerGroup} 6"
				class="sapUiTinyMarginTop"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					src="sap-icon://hint"
					decorative="false"
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					tooltip="{i18n>tooltipCustomerGroup6}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					selectedKey="{customer>/customerGroup/6}"
					items="{customerGroups>/}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				text="{i18n>lblCustomerGroup} 7"
				class="sapUiTinyMarginTop"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					src="sap-icon://hint"
					decorative="false"
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					tooltip="{i18n>tooltipCustomerGroup7}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					selectedKey="{customer>/customerGroup/7}"
					items="{customerGroups>/}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				text="{i18n>lblCustomerGroup} 8"
				class="sapUiTinyMarginTop"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					src="sap-icon://hint"
					decorative="false"
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					tooltip="{i18n>tooltipCustomerGroup8}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					selectedKey="{customer>/customerGroup/8}"
					items="{customerGroups>/}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				text="{i18n>lblCustomerGroup} 9"
				class="sapUiTinyMarginTop"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					src="sap-icon://hint"
					decorative="false"
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					tooltip="{i18n>tooltipCustomerGroup9}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					selectedKey="{customer>/customerGroup/9}"
					items="{customerGroups>/}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				text="{i18n>lblCustomerGroup} 10"
				class="sapUiTinyMarginTop"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					src="sap-icon://hint"
					decorative="false"
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					tooltip="{i18n>tooltipCustomerGroup10}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					selectedKey="{customer>/customerGroup/10}"
					items="{customerGroups>/}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox> -->

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 11"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup11}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/11}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 12"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup12}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/12}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 13"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup13}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/13}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 14"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup14}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/14}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 15"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup15}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/15}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 16"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup16}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/16}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 17"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup17}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/17}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 18"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup18}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/18}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 19"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup19}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/19}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblCustomerGroup} 20"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipCustomerGroup20}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerGroups>/}"
					selectedKey="{customer>/customerGroup/20}"
					width="100%">
					<core:Item key="{customerGroups>key}" text="{customerGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblPriceGroup}"
				visible="{view>/show/salesFields}"/>
			<HBox visible="{view>/show/salesFields}">
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipPriceGroup}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{GWD_C_FORM_UI5>/ZFA1_KONDASet}"
					selectedKey="{customer>/customerGroup/priceGroup}"
					width="100%">
					<core:Item key="{GWD_C_FORM_UI5>Konda}" text="{GWD_C_FORM_UI5>Vtext}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				text="{i18n>lblPriceListType}"/>
			<HBox>
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipPriceListType}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{GWD_C_FORM_UI5>/ZFA1_PLTYPSet}"
					selectedKey="{customer>/customerGroup/priceListType}"
					width="100%">
					<core:Item key="{GWD_C_FORM_UI5>Pltyp}" text="{GWD_C_FORM_UI5>Ptext}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

			<Label
				class="sapUiTinyMarginTop"
				required="true"
				text="{i18n>lblStatisticsGroup}"/>
			<HBox>
				<core:Icon
					class="sapUiTinyMarginTop sapUiTinyMarginEnd"
					decorative="false"
					src="sap-icon://hint"
					tooltip="{i18n>tooltipStatisticsGroup}">
					<core:layoutData>
						<FlexItemData growFactor="0"/>
					</core:layoutData>
				</core:Icon>
				<Select
					change="onToggleValidationButton"
					enabled="{= ${view>/enabled/secondary} &amp;&amp; !${view>/enabled/existing} }"
					forceSelection="false"
					items="{customerStatisticsGroups>/}"
					selectedKey="{customer>/customerGroup/statisticsGroup}"
					valueState="{= ${customer>/customerGroup/statisticsGroup} ? 'None' : 'Error' }"
					width="100%">
					<core:Item key="{customerStatisticsGroups>key}" text="{customerStatisticsGroups>text}"/>
					<layoutData>
						<FlexItemData growFactor="1"/>
					</layoutData>
				</Select>
			</HBox>

		</forms:content>
	</forms:SimpleForm>
</core:FragmentDefinition>
